@if (Request.IsAuthenticated)
{
    @Html.ActionLink("(" + User.Identity.Name + ")", "UserProfile", "Account")
    @Html.ActionLink("Sign Out", "LogOut", "Account")
}
else
{
    @Html.ActionLink("Login Here", "Login", "Account")
}
